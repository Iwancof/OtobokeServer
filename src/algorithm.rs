#![allow(unused)]

use std::fs;
use std::fmt;
use super::map::{
    MapProcAsGame,
    QuanCoord,
};
use std::ops::Sub;

impl MapProcAsGame { // for AI
    // Easy AI
    pub fn update_target(&mut self) -> usize {
        0
    }

}


